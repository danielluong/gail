includes:
    - vendor/larastan/larastan/extension.neon
    - phpstan-baseline.neon

parameters:
    level: 5

    treatPhpDocTypesAsCertain: false

    paths:
        - app
        - config
        - database
        - routes

    excludePaths:
        - database/migrations/*
