/*
 * Intentionally empty. Shared Inertia props injected by
 * HandleInertiaRequests::share() are accessed through useSharedProps()
 * — see resources/js/hooks/use-shared-props.ts for the typed surface.
 */
export {};
